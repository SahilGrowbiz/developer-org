name: Deploy and Run Tests  

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Salesforce CLI
        run: |
          sudo npm install -g sfdx-cli
          sfdx --version
          sfdx plugins --core

      - name: Debug - Verify secrets are available
        run: |
          echo "SF_CLIENT_ID length: ${#SF_CLIENT_ID} chars"
          echo "SF_USERNAME: $SF_USERNAME"
          echo "SF_LOGIN_URL: $SF_LOGIN_URL"
          echo "SF_JWT_KEY length: ${#SF_JWT_KEY} chars"
        env:
          SF_CLIENT_ID: ${{ secrets.SF_CLIENT_ID }}
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SF_LOGIN_URL: ${{ secrets.SF_LOGIN_URL }}
          SF_JWT_KEY: ${{ secrets.SF_JWT_KEY }}

      - name: Authenticate with JWT
        env:
          SF_JWT_KEY: ${{ secrets.SF_JWT_KEY }}
          SF_CLIENT_ID: ${{ secrets.SF_CLIENT_ID }}
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SF_LOGIN_URL: ${{ secrets.SF_LOGIN_URL }}
        run: |
          # Create key file
          echo "$SF_JWT_KEY" | base64 --decode > server.key
          chmod 600 server.key
          
          # Debug key file
          echo "JWT Key file info:"
          ls -l server.key
          head -n 1 server.key
          tail -n 1 server.key
          
          # Execute auth command (single line, no echo)
          SFDX_DEBUG=1 sfdx force:auth:jwt:grant \
            --clientid "$SF_CLIENT_ID" \
            --jwtkeyfile server.key \
            --username "$SF_USERNAME" \
            --instanceurl "$SF_LOGIN_URL" \
            --setalias deploy-org \
            --json \
            --loglevel trace
          
          # Verify authentication
          sfdx force:org:list --all

      - name: Run All Test Classes
        run: |
          sfdx force:apex:test:run \
            --codecoverage \
            --resultformat human \
            --outputdir test-results \
            --wait 30 \
            --targetusername deploy-org

      - name: Deploy to Salesforce
        run: |
          sfdx force:source:deploy \
            --sourcepath force-app \
            --targetusername deploy-org \
            --checkonly